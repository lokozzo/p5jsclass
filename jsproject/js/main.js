function setup() {
	createCanvas(400, 400)
}
function draw(){
	background(255, 23, 80);
	fill(0, 255, 0);
	// ellipse(x, y, width, height);
	// rect(x, y, width, height);
	triangle(30, 30, 20, 40, 21, 23 );
}